# 최신 개발서버 적용본 인계

## 2026-09-07 최신 main 통합

아래의 최초 인계 기록보다 이 항목과 [최신 통합 기록](releases/2026-09-07-main-sync.md)이 우선합니다.

- 사용자 승인에 따라 최신 개발 배포 `dpl_7S8QaxRSvvb4eySABNEHBJySut9M`의 소스를 기존 `eb62600` 이후에 통합해 main에 반영합니다. 기존 커밋 이력은 보존합니다.
- 대시보드 단일 overview 조회, 중복 인증/학교정보 요청 개선, 서울 icn1 함수 지역, 실시간 심박 30자리와 짧은 수신 공백 처리, 최신 디자인 세 가지와 Heart Care 명칭을 포함합니다.
- 배포 manifest 271개 중 비실행 임시자료 5개는 제외합니다. 나머지 266개 중 265개는 배포본과 바이트 단위로 같고, `exercise-data.test.mjs`만 최신 overview 동작에 맞춰 테스트를 갱신했습니다. 실행 소스는 배포본과 동일합니다.
- Node 22와 잠금 파일 기준으로 검증합니다. 독립적인 테스트 및 합성 미리보기는 포함하되, 비공개 형제 폴더의 기준본을 요구하는 비교 검사는 공개 인계에서 제외합니다.
- 기존 4구간/64·77·96% 심박 계산 규칙은 변경하지 않았습니다. 이것이 국가 교육과정의 의무 기준이라는 뜻은 아닙니다. 교육적 활용 기준은 별도 조사 문서에서 다룹니다.
- 과거의 '성능 개선 보류', '함수 iad1', 'main 미반영' 설명은 아래 최초 인계 당시의 이력입니다. 실제 학교 규모 부하, 실물 기기 및 프린터 확인은 여전히 별도 검증 사항입니다.

## 최초 인계 기준

- Git 대상: `atvserver001-dotcom/sams_project_test`, 디자인 작업 브랜치 `codex/sams-redesign-20260905`.
- 공개 저장소 부모 커밋: `03347ff284d40f30214e9241504b03ddef6ce057`.
- 서버 기능 보존에 사용한 기존 라이브 기준: `5e55cdd381cb6397b0a62ad4999492f3542a8f6b`. 공개 저장소의 과거 main과 다른 시점이며, 최신 배포본의 기능 코드를 함께 포함했습니다.
- 2026-09-07 재확인한 개발 배포: `dpl_FaWTNePWAb6NcTZkeSa23xtBeJxJ`, READY.
- Git 푸시는 개발 결과물 인계입니다. main 병합, PR 생성, 운영 서버 적용, DB 변경을 포함하지 않습니다.

## 포함 내용

학교·관리자 페이지의 승인된 공통 디자인, 차트·출력 화면, 기존 API·인증·측정 기능을 보존한 통합 소스입니다. 학교 일반 메뉴에서 JumpRope/BodyComposition 임시 항목을 제외하고 최하단 개발중 그룹의 줄넘기 / 체성분 테스트로 정리한 후속 수정까지 포함합니다.

React/Next.js, Tailwind·공통 UI 컴포넌트, Pretendard와 Lucide 기반입니다. 관리자 파란 테마와 학교 빨간 테마를 유지합니다. 이번 Git 정리 과정에서 배포된 디자인이나 업무 로직을 재작성하지 않았습니다.

## 배포본 일치와 제외 자료

[배포 manifest](releases/2026-09-06-deployed-source-manifest.json)의 263개 파일 중 다음 비실행 자료 5개는 새 공개 인계에서 제외했습니다. 나머지 258개는 배포본과 SHA-256이 일치하는 소스로 준비했습니다.

- `scripts/mcp.err.log`, `scripts/mcp.out.log`: 빈 임시 로그.
- `supabase/.temp/cli-latest`: 로컬 도구 캐시.
- `src/components/exercises/session-log-2026-09.md`, `src/lib/heart-rate/session-log-2026-09.md`: 로컬 작업 이력.

추가로 합성 미리보기 소스, 테스트 설정·보조 파일, 그래프 검수 도구 소스·폰트 라이선스 및 이 인계 문서를 포함했습니다. 환경 비밀 값, `.vercel` 로컬 연결, 새 도구 개인 설정, 실제 학생 측정 스냅샷·화면 캡처, 의존성 설치 폴더와 빌드 산출물은 추가하지 않았습니다. 부모 커밋에 이미 있던 파일·이력은 이번 작업에서 임의 정리하거나 재작성하지 않았습니다.

## 검증과 알려진 제한

- Node 22, 잠금 파일과 동일한 Next/React/Vitest/TypeScript 버전으로 기본 테스트 187개, 별도 메뉴·미리보기·운동기록 테스트 115개, TypeScript PASS.
- 기존 배포 단계에서 수행한 계약·FHD/좁은 화면 확인은 별도 릴리스 기록이며, 이번 Git 푸시에서 실제 기기·DB 쓰기·인쇄를 재검증하지 않았습니다.
- 과거 차트랩 테스트는 10 PASS / 10 FAIL입니다. 현재 4구간 심박 분류와 이전 5구간 실험 기대값 및 유효 범위의 차이가 원래 통합 폴더에서도 동일하게 나타납니다. 최신 배포본 보존을 위해 수정하지 않았습니다.
- 과거 `deployment-preservation.test.cjs`, `device-test-ui.test.cjs`, `navigation-retained-pages.test.cjs`는 외부 형제 폴더의 고정 라이브 기준을 요구합니다. 그 비공개 기준 폴더와 의존 검사는 이번 공개 인계에 추가하지 않았습니다. 기본 `npm test`와 구분합니다.
- 시각 기준 이미지의 전체 승인, 실제 기기 측정, 학교 PC·실물 프린터 및 운영 부하 검증은 완료된 것으로 주장하지 않습니다.

## 운영 반영 전 필수 확인

현재 개발 서버 성능 개선은 사용자 판단에 따라 보류했습니다. 운영에 그대로 옮겨도 문제가 없다는 의미가 아닙니다.

1. 대시보드의 학급별 반복 요청을 일괄 집계 구조로 검토하고 집계값을 대조합니다.
2. 학교 정보의 중복 요청과 초기 인증 이후의 단계적 대기를 정리하되 권한·학교별 데이터 격리를 유지합니다.
3. API 함수와 DB 리전을 함께 확인합니다. 개발 함수는 미국 iad1이며 DB 위치는 별도 확인 대상입니다.
4. 실제 규모 학교 데이터와 동시 접속 조건에서 조회 시간을 측정하고 합의한 목표로 승인합니다.
5. 과거 차트 실험과 현재 측정 규칙의 불일치를 개발자와 정리하고 실제 그래프·결측값·0측정값을 다시 검증합니다.
