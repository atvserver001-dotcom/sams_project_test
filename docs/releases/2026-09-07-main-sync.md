# 2026-09-07 최신 개발 배포 main 통합

## 요청 및 경로

- 사용자 승인: 최신 배포 변경을 main 브랜치에 적용한다.
- 대상 저장소: `atvserver001-dotcom/sams_project_test`.
- 보존한 main 시작점: `03347ff284d40f30214e9241504b03ddef6ce057`.
- 선행 디자인 커밋: `eb626004867436e7aba6a8d756dc224cab5b8203`.
- 통합 브랜치: `codex/main-sync-20260907`. 기존 작업 폴더의 변경은 되돌리지 않는다.
- 서버 코드 기준: `dpl_7S8QaxRSvvb4eySABNEHBJySut9M`, 개발 주소 https://sams-project-test.vercel.app.
- Git 반영과 서버 배포는 구분한다. 이번 통합에서 운영서비스, 데이터베이스, 기기 측정 또는 심박 계산식을 변경하지 않는다.

## 배포 소스 대조

- 배포 manifest SHA-256: `8881AE3153E580C2F0E872D6A40C4707846B52EE8292691B0F0864575C1C49D6`.
- 원본 manifest의 271개 파일을 확인한 뒤 통합했다. 아래 비실행 자료 5개는 최초 Git 인계와 동일하게 제외한다.
  - `scripts/mcp.err.log`, `scripts/mcp.out.log`
  - `supabase/.temp/cli-latest`
  - `src/components/exercises/session-log-2026-09.md`
  - `src/lib/heart-rate/session-log-2026-09.md`
- 나머지 266개 중 265개는 배포본과 정확히 같고, `src/components/exercises/exercise-data.test.mjs`는 오래된 4-worker 기대값을 현재 단일 overview GET 계약 검증으로 갱신했다. 0과 결측값 보존, 조회 취소 signal 전달도 검사한다.
- 추가 테스트는 요약 API, 배포 guard, 최신 레이아웃 및 합성 미리보기다. 비공개 형제 기준본에 의존하는 네 비교 검사는 커밋하지 않는다.
- 비밀 값, 실제 학생 측정 스냅샷, 의존성 설치 폴더, 빌드 산출물 및 로컬 도구 설정은 새로 게시하지 않는다.

## 포함한 실행 변경

- 대시보드의 단일 요약 API 및 운동기록 집계/분할 조회.
- 인증 및 학교정보 요청 처리 개선과 기존 접근 통제 보존.
- Vercel 함수 지역 `icn1`, 기존 개발 프로젝트/DB/기기 API 비활성/허용 운영자 guard 유지.
- 실시간 심박 1~30번 자리 및 짧은 수신 공백 표시 처리.
- 대시보드 학년도 드롭다운, 중단 카드 높이 일치, 포커스 파형 잔여 높이 활용.
- Heart Care 메뉴/제목 및 기존 콘텐츠 별칭 유지.

## 검증

- Node 22.23.2, `npm ci`로 잠금 파일 의존성을 설치했다.
- Vitest: 28파일 219개 통과. TypeScript 검사 통과.
- 별도 메뉴/운동기록/미리보기/개발 배포 guard 테스트: 121개 통과.
- 합성 브라우저 성능/인증 테스트: 2개 통과. 검토 중 발견한 이전 화면 제목 기대값을 Heart Care로 갱신했다.
- 커밋 전 Git 인덱스의 파일 내용으로 다시 대조해 배포본과 265파일 일치, 테스트 1파일 변경, 비실행 자료 5개 제외를 확인했다. 삭제 파일은 없다. UTF-8 엄격 검사를 통과했다.
- 로컬 Next.js 빌드 통과, 22페이지 생성. 외부 DB에 연결하지 않는 `build-check.invalid` 및 가짜 키를 프로세스 환경에만 사용했다. 기존 lint 경고는 남아 있다.
- 심박 분류를 5구간으로 기대하는 과거 차트랩과 외부 기준본 비교 검사는 이번 통합의 통과 검증에 포함하지 않는다. 이전 실패 이력을 승인 처리하지 않는다.
- 현재 작업은 배포 코드의 Git 동기화다. 실제 기기, DB 쓰기, 출력물 및 전체 페이지 UI 검증을 새로 수행한 것으로 주장하지 않는다.
